apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: k6-scripts
  namespace: flagger-system
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
