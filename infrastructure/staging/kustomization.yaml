apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  #- flagger
  #- gateway-api

patches:
  - path: patches/gateway-api/gateway-patch.yaml
    target:
      kind: Gateway
      name: gateway-api
  # - path: flagger/helmrelease-patch.yaml
  #   target:
  #     kind: HelmRelease
  #     name: flagger
