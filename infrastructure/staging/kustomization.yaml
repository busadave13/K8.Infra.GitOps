apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  #- gateway-api

patches:
  - path: patches/gateway-api/gateway-patch.yaml
    target:
      kind: Gateway
      name: gateway-api