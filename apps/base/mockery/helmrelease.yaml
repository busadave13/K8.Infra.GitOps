apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mockery
  namespace: flux-system
spec:
  interval: 2m
  chart:
    spec:
      chart: mockery
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: github
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
