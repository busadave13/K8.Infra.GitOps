apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #- ../base
  #- ../base/repos
  #- ../base/fluxcd
  #- ../base/weather
  #- ../base/mockery
  - ../base/podinfo

patches:
  - path: patches/podinfo-httproute-patch.yaml
    target:
      kind: HTTPRoute
      name: podinfo
